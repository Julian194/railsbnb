<h1><%= @listing.title %></h1>
<%= render partial: "booking_form", locals: { listing: @listing } %>
<div>
  Per night <%= number_to_currency(@listing.nightly_price / 100, precision: 0) %>
</div>
<div>
  Cleaning fee <%= number_to_currency(@listing.cleaning_fee / 100, precision: 0) %>
</div>
<%= @listing.city %>, <%= @listing.state %>
<p>
  <%= @listing.about %>
</p>
<p>
  <%= @listing.max_guests %>
</p>
<% if @listing.photos.exists? %>
  <% @listing.photos.each do |photo| %>
    <%= image_tag photo.photo.variant(resize_to_limit: [100, 100]) %>
  <% end %>
<% end %>
<ul>
  <% @listing.rooms.each do |room|  %>
    <li><%= room.room_type %> beds: <%= room.beds %></li>
  <% end %>
</p>
<div>
  <h4>Host</h4>
  <%= @listing.host.email %>
</div>